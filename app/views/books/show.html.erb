<h1>Book detail</h1>

<table>
      <tr>
        <td>
          <img><%= %></img>
          <%= link_to, users_path(user.id) %>
          <a><%= user.name %></a>
        <td>
          <%= link_to
          <span><%= book.title %></span>
        </td>
        <td>
          <span><%= book.body %></span>
        </td>
      </tr>
    <% end %>
  </table>